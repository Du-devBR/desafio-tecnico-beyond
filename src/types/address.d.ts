export interface AddressShipping {
  id: string;
  planet: string;
  product: string;
  storage_code: string;
  zip_code: string;
  phone: string;
  country?: string;
  state?: string;
  city?: string;
  address?: string;
  location?: string;
}
